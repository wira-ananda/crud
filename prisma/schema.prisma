generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Pasien {
  noPasien  Int    @id @default(autoincrement())
  nama      String
  keluhan   String
  kunjungan Int
}
